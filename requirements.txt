# ------------------------------------------------------------------
# 运行依赖
# ------------------------------------------------------------------
pystray>=0.19
Pillow>=9.0

# ------------------------------------------------------------------
# 打包 exe 时才需要（pip install -r requirements.txt 会一并装上）
# ------------------------------------------------------------------
pyinstaller>=6.0
